# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored and stays on your machine only.
# Add any environment variables the app needs below.

# Example:
# NEXT_PUBLIC_API_URL=http://localhost:3000
